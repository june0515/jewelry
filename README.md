# Jewelry Wardrobe PWA

个人首饰管理网页 App。支持本地 IndexedDB 存储、图片上传压缩、筛选、佩戴次数记录、JSON 导出、PWA 离线访问。

## 本地运行
```bash
npm install
npm run dev
```

## 部署 Vercel
Framework: Vite
Build Command: npm run build
Output Directory: dist
